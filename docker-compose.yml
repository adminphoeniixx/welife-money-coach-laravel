services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_ENV: production
    image: money:latest
    ports:
      - "8080:8001"
    environment:
      CONTAINER_MODE: http
      OCTANE_SERVER: frankenphp
      RUNNING_MIGRATIONS_AND_SEEDERS: "true"
      WITH_SCHEDULER: "true"
      # App
      APP_NAME: MoneyCoach
      APP_ENV: production
      APP_DEBUG: "false"
      APP_KEY: ${APP_KEY}
      APP_URL: ${APP_URL:-http://localhost:8080}
      # Postgres (values come from .env). Use the internal host/port when the
      # app runs on the same server/network as the database.
      DB_CONNECTION: pgsql
      DB_HOST: ${DB_HOST}
      DB_PORT: ${DB_PORT:-5432}
      DB_DATABASE: ${DB_DATABASE}
      DB_USERNAME: ${DB_USERNAME}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_SSLMODE: ${DB_SSLMODE:-disable}
      # Queue / cache / session use the database (Postgres) by default
      QUEUE_CONNECTION: database
      CACHE_STORE: database
      SESSION_DRIVER: database
    # Uploaded proofs, debt documents and vault files are encrypted blobs on
    # the local disk. Without a persistent mount they live inside the
    # container and every redeploy wipes them, leaving rows in Postgres whose
    # files are gone (the API then 404s those links).
    volumes:
      - money-storage:/var/www/html/storage/app/private
    restart: unless-stopped

volumes:
  money-storage:
